{
  "private": true,
  "workspaces": ["jude-map", "jude-tf"],
  "scripts": {
    "version": "changeset version",
    "publish-packages": "changeset publish"
  },
  "devDependencies": {
    "@changesets/cli": "^2.30.0"
  }
}